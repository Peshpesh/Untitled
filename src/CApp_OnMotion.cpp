#include "CApp.h"

void CApp::OnMotion()
{
	/*
	SDL_PumpEvents();
	if (state[SDL_SCANCODE_LEFT])
		CCamera::CameraControl.OnMove(3, 0);
	if (state[SDL_SCANCODE_UP])
		CCamera::CameraControl.OnMove(0, 3);
	if (state[SDL_SCANCODE_DOWN])
		CCamera::CameraControl.OnMove(0, -3);
	if (state[SDL_SCANCODE_RIGHT])
		CCamera::CameraControl.OnMove(-3, 0);
	*/
}
